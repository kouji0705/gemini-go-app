version: "3.8"

services:
  app:
    build: .
    container_name: gemini-go-app
    volumes:
      - .:/app
    env_file:
      - .env
    # --- 変更点 ---
    # コンテナを終了させずに常駐させる設定
    tty: true
    # 何もしないで待機し続けるコマンド
    command: tail -f /dev/null
